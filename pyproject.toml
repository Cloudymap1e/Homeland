[project]
name = "homeland"
version = "0.1.0"
description = "Homeland river-defense prototype"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
